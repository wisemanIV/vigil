{
  "type": "object",
  "properties": {
    "bulkEmailThreshold": {
      "type": "number",
      "minimum": 1,
      "maximum": 50,
      "default": 3,
      "description": "Number of unique emails that triggers bulk detection"
    },
    "bulkNameThreshold": {
      "type": "number",
      "minimum": 1,
      "maximum": 100,
      "default": 5,
      "description": "Number of names that triggers bulk detection"
    },
    "structuredDataThreshold": {
      "type": "number",
      "minimum": 1,
      "maximum": 100,
      "default": 10,
      "description": "Number of structured rows that triggers detection"
    },
    "allowInternalBulkData": {
      "type": "boolean",
      "default": false,
      "description": "Allow bulk data transfers on internal domains"
    }
  }
}
